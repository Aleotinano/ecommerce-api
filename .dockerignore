node_modules
.git
.github

# env files: se pasan por compose/--env-file, nunca al build context
.env
.env.*

tests
*.test.js
coverage

docs
front-md-guia
tmp
generated
backups

# Infra del deploy: la consume docker/compose desde el host, no va adentro de la
# imagen.
docker-compose*.yml

.claude
.agents
*.log

# excepciones VEX de Docker Scout: se consumen desde el host con
# `docker scout cves --vex-location .vex`, no van adentro de la imagen
.vex
